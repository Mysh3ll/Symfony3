{% extends 'base.html.twig' %}
{% block content %}
    <!-- Page Content -->
    <div class="container">

    <hr>
    <hr>

    <div class="row">
        <div class="col-sm-12">
            <div class="container well">
                <div class="col-md-9">
                    <h2>Information de l'événement</h2>
                    <br>
                    <div class="col-md-6">
                        <dl>
                            <dt>Titre</dt>
                            <dd>
                                {{ Event.titreEvent }}
                                &nbsp;
                            </dd>
                            <br>

                            <dt>Date</dt>
                            <dd>
                                {{ Event.dateEvent|date("d/m/Y", "Europe/Paris") }}
                                &nbsp;
                            </dd>
                            <br>

                            <dt>Catégorie</dt>
                            <dd>
                                {{ Event.idType.libelleType }}
                                &nbsp;
                            </dd>
                            <br>

                            <dt>Salle</dt>
                            <dd>
                                {{ Event.idSalle.nomSalle }}
                                &nbsp;
                            </dd>
                            <br>
                        </dl>
                    </div>
                    <div class="col-md-3">
                        <dt>Affiche</dt>
                            <dd>
                                <img class="img-responsive" src="{{ vich_uploader_asset(Event, 'imageFile') }}" alt="aucune affiche"/>
                                &nbsp;
                            </dd>
                    </div>
                </div>
                <div class="col-md-3">
                    <h3>Actions :</h3>
                    <p>
                        {# INPUT EDIT #}
                        <a href="{{ path('admin_event_edit', {'id':Event.idEvent}) }}" class="btn btn-primary btn-lg btn-block">
                            <span class="glyphicon glyphicon-edit"></span>
                            Modifier
                        </a>
                    </p>
                    <p>
                        {# INPUT CANCEL #}
                        <a href="{{ path('admin_event_list') }}">
                            <button type="button" class="btn btn-danger btn-lg btn-block">
                                <span class="glyphicon glyphicon-repeat"></span>
                                Annuler
                            </button>
                        </a>
                    </p>
                    {# FORM DELETE #}
                    <p>
                        {{ include('@App/backOffice/forms/form.html.twig', { form: delete_form, message: 'Cette opération est irréversible, veuillez confirmer votre choix.'}) }}
                    </p>
                </div>
            </div>
        </div>
    </div>
    <!-- /.row -->
{% endblock %}
{% block javascripts %}
<script src="{{ asset('js/bootbox.min.js') }}"></script>
{% endblock %}